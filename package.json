{
  "name": "@markdomkan/in-memory-bus",
  "version": "2.2.0",
  "description": "A simple event bus for JavaScript & TypeScript",
  "main": "dist/main.js",
  "types": "dist/main.d.ts",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/markdomkan/in-memory-bus.git"
  },
  "scripts": {
    "test": "bun test",
    "build": "tsc ./main.ts --declaration --skipLibCheck --lib es6 --target es6 --outDir ./dist ",
    "publish": "bun run build && npm publish --access public"
  },
  "engines": {
    "bun": ">=1.1"
  },
  "author": "Mark Domkan",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/markdomkan/in-memory-bus/issues"
  },
  "homepage": "https://github.com/markdomkan/in-memory-bus#readme",
  "devDependencies": {
    "@types/bun": "^1.1.13",
    "typescript": "^5.6.3"
  }
}